@model List<Plot>

<h2>Entered Points</h2>
<form asp-action="AddPoint" method="post">
  <label for="X">X:</label>
  <input type="number" name="X" required>
  <label for="Y">Y:</label>
  <input type="number" name="Y" required>
  <button type="submit">Add Point</button>
</form>

<h2>Achieved Plots</h2>
@foreach (var plot in Model)
{
  <div>
  <p>Equation: @plot.Equation</p>
  <img src="data:image/png;base64,@Convert.ToBase64String(plot.PlotImage)" alt="Plot Image">
</div>
}